import 'package:flutter/material.dart';

class CriarTarefas extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Criarei aqui"),
      ),
    );
  }
}
